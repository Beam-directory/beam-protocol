# Beam 1.7.0 COPPEN Pilot Live Status

Status: `live-read-only / business-review-pending`

Last verified: `2026-08-23T11:56:30Z`

## Live result

- The public Directory is running candidate `1.7.0-coppen-rc.1` from Git SHA `8008bc2cc081e5c0c231237c6740a656c880b159`.
- The organization `COPPEN GmbH` owns the verified Beam domain `coppen.beam.directory`; its DNS proof is publicly resolvable under `coppen.de`.
- `grok@coppen.beam.directory` (`did:beam:coppen:grok`) and `coppen-assistant@coppen.beam.directory` (`did:beam:coppen:coppen-assistant`) are issued and domain-verified.
- The hosted MCP at `https://mcp.beam.directory/mcp` now runs as `grok@coppen.beam.directory`.
- OAuth authorization-code + PKCE (`S256`) passed against the hosted identity provider with the exact MCP audience and only `beam:read`.
- The advertised tool set is limited to `beam_status` and `beam_prepare_handoff`; `beam_status` succeeded, `beam_send` is absent, and no message was sent.

## Business identity review

- Target: `coppen-assistant@coppen.beam.directory`
- Submitted legal identity: `COPPEN GmbH`, Germany, `HRB 68658`.
- Primary register evidence: official Handelsregister current record, Amtsgericht Ludwigshafen am Rhein, seat Bad Duerkheim.
- Directory verification request: `1`, status `pending`, created `2026-08-23T11:48:18.665Z`.
- The agent remains at tier `verified` until an authenticated independent admin review approves the evidence.

## Open gates

- Admin magic-link sign-in and the independent business-review decision are still required before the target reaches tier `business`.
- One synthetic, non-customer-data Grok invocation is still required to prove the connector sees the new COPPEN identity and prepares a handoff to the business-tier target.
- The dashboard candidate was not deployed because the configured GitHub Vercel token was rejected; the existing production dashboard remains unchanged.
- The Keycloak image still has 14 high/critical scanner findings. A documented, time-bounded pilot risk decision is required before any broader release.
- This internal COPPEN run does not satisfy the separate external-operator dogfood gate for the public `1.7.0` release.

## Safety boundary

- Read-only MCP only.
- No customer data used.
- No Beam message sent.
- No public release tag, merge, announcement, or external partner onboarding performed.
- Credentials and OAuth smoke artifacts are stored outside the repository with restrictive file permissions.
