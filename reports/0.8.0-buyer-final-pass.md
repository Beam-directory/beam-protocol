# Beam 0.8.0 Buyer Final Pass

## Context

- run date: `2026-03-31`
- repo branch at run time: `codex/0-8-0-final-cut`
- live surfaces used:
  - `https://beam.directory/`
  - `https://beam.directory/guided-evaluation.html`
  - `https://beam.directory/hosted-beta.html`
  - `https://docs.beam.directory/guide/design-partner-onboarding`
  - `https://api.beam.directory/analytics/events`
  - `https://api.beam.directory/waitlist`
  - `https://api.beam.directory/health`
  - `https://api.beam.directory/stats`
  - `https://api.beam.directory/release`
- live API release truth during the run:
  - version: `0.8.0`
  - git SHA: `337d893c44c8bc818f24c5c29639712ab31fda1a`
  - deployed at: `2026-03-31T00:13:40.000Z`

## Run

### 1. Landing page comprehension

The live landing page opens on the plain-language headline:

- `Beam | Safe AI Work Between Companies`

It still pushes the buyer into one obvious next step:

- hero CTA: `See the guided evaluation`
- secondary CTA: `Request hosted beta`
- the first-party analytics script is present on the page

### 2. Guided evaluation path

The guided evaluation page is live and consistent with the landing copy:

- primary CTA: `Request a guided pilot`
- secondary CTA: `Request hosted beta`
- the first-party analytics script is present here as well

This keeps the public funnel narrow: explanation first, then one guided pilot request.

### 3. Hosted beta intake

The hosted beta page is live and correctly framed around one narrow workflow:

- headline: `Request a guided Beam pilot for one real workflow.`
- the workflow field is present
- the copy still reinforces narrow scope and one owner

### 4. First-party analytics check

A real analytics event posted successfully to the live API with a first-party `Origin` header:

```json
{
  "ok": true,
  "event": {
    "id": 3,
    "sessionId": "buyer-final-337d893",
    "origin": "https://beam.directory",
    "pageKey": "guided_evaluation",
    "eventCategory": "cta_click",
    "ctaKey": "guided_eval_hosted_beta_hero",
    "targetPage": "hosted_beta",
    "createdAt": "2026-03-31T00:16:16.787Z"
  }
}
```

### 5. Hosted beta request check

A real hosted beta intake request was accepted on the live API:

```json
{
  "status": "registered",
  "email": "beam-080-final-pass@northwind.example",
  "company": "Northwind Example",
  "workflowType": "hosted-beta-partner-handoff",
  "requestStatus": "new",
  "nextStep": "Beam will review the workflow, assign an owner, and follow up with the next concrete step."
}
```

Important detail:

- the request is not dropped on the floor
- the response includes the canonical request object
- the response includes the operator-facing next step immediately

### 6. Docs alignment

The onboarding pack remains live and aligned with the same hosted-beta story. The public path still reads as:

- understand Beam
- open guided evaluation
- request one narrow pilot
- continue with the onboarding pack if the pilot is real

## Result

`PASS`

The buyer-like path is ready for a real external first conversation on the live `0.8.0` candidate.
