# 0.7.0 Clean-Start Onboarding

Generated at: 2026-03-30T14:54:52Z

## Goal

Validate the documented hosted-demo onboarding path from a clean local stack:

1. copy `ops/quickstart/.env.example`
2. `docker compose ... down -v --remove-orphans`
3. `docker compose ... up -d --build`
4. `npm run demo:seed`
5. `npm run demo:run`
6. `npm run quickstart:smoke`
7. `npm run dogfood:hosted-demo`

## Measured Run

Fresh volumes were used for this run. The repo checkout and Docker build cache were already present locally, so these timings represent a clean stack bring-up rather than a cold clone from the network.

| Step | Seconds | Result |
| --- | ---: | --- |
| `prepare_env` | 0.00 | pass |
| `down_clean` | 1.14 | pass |
| `up_build` | 13.19 | pass |
| `demo_seed` | 0.27 | pass |
| `demo_run` | 0.42 | pass |
| `quickstart_smoke` | 0.39 | pass |
| `dogfood_hosted_demo` | 0.40 | pass |
| **Total** | **15.81** | **pass** |

## Key Checks

- Quickstart defaults now bind to `43100`, `43173`, `43220`, and `43290` to avoid common local port collisions.
- Hosted demo seeding is idempotent against an already-seeded directory; repeated `demo:seed` and `demo:run` calls no longer consume duplicate registration quota.
- The clean-start run finished with `agent-registration:ip:unknown = 4`, which matches the four seeded demo identities instead of the previous repeated-registration burst.
- `reports/0.7.0-hosted-demo-readiness.md` was generated from the same stack and recorded zero alerts and zero dead letters.

## Outcome

The hosted-demo quickstart is now in a state where the documented onboarding path can be executed end-to-end on a fresh local stack without manual intervention.
