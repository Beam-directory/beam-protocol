# Beam 1.7.0 UI Smoke

Status: `pass`

Last updated: `2026-04-03`

## Summary

Beam `1.7.0` now treats the operator-facing dashboard as part of the hosted-adoption surface instead of a nice-to-have.

This smoke proves the core pages load, authenticate, and remain visually within the committed baseline envelope:

- login
- OpenClaw fleet
- workspace
- intents
- trace detail

## Commands

Verified locally on `2026-04-03`:

- `npm run quickstart:ui-smoke`
- `npm run quickstart:ui-compare -- --latest`
- `npm run quickstart:ui-report -- --latest --output tmp/dashboard-ui-smoke/report.md`

## Latest Run

- Screenshot directory: `/Users/tobik/Documents/BEAM/beam-protocol/tmp/dashboard-ui-smoke/2026-04-03T11-47-30-669Z`
- Baseline directory: `/Users/tobik/Documents/BEAM/beam-protocol/spec/visual/dashboard-baselines`
- Allowed diff ratio: `8.00%`

## Visual Regression Result

- `loginDesktop`: `PASS` (`0.00%` changed)
- `fleetDesktop`: `PASS` (`0.02%` changed)
- `workspaceDesktop`: `PASS` (`0.00%` changed)
- `intentsDesktop`: `PASS` (`0.01%` changed)
- `traceDesktop`: `PASS` (`0.00%` changed)

## Notes

- The repo-owned CI `quickstart` job now runs the same compare/report flow and uploads the UI proof as the `quickstart-ui-smoke` artifact.
- The baseline is intentionally tolerant to small runtime rendering differences while still catching real layout or routing regressions.
