# Beam 1.2.0 RC1 Checklist

## Context

- target: OpenClaw fleet connector release
- status: `released`
- branch: `main`
- release tag: `v1.2.0`
- updated: `2026-04-02`

## Checklist

- [x] buyer dry run passes with the OpenClaw fleet onboarding, setup, status, and workspace surfaces (`reports/1.2.0-buyer-dry-run.md`)
- [x] operator dry run proves three approved hosts, live route health, duplicate conflict handling, and revoke remediation (`reports/1.2.0-operator-dry-run.md`)
- [x] fleet smoke proves real three-host send/receive, duplicate conflict blocking, and revoked-host delivery denial (`reports/1.2.0-fleet-smoke.md`)
- [x] three approved hosts are visible from one central Beam fleet view
- [x] each host publishes inventory and heartbeat successfully
- [x] at least one real inbound and outbound message works on every host in the fleet smoke ring
- [x] duplicate identity conflict detection blocks delivery until the conflicting host is revoked
- [x] revoking a host disables its routes immediately and surfaces revoked health in the fleet and workspace views
- [x] workspace identities expose host badges, route source, runtime session state, and Beam-managed identity control
- [x] full repo verification passes on the same branch candidate (`npm run build`, `npm test`, `npm run test:e2e`, `npm -C docs run build`)
- [x] release notes draft exists (`reports/1.2.0-release-notes-draft.md`)
- [x] changelog contains a `v1.2.0 (draft)` block
- [x] tag-time release smoke passes after publish and deploy (`reports/1.2.0-release-smoke.md`)

## Release Gates

1. Full repo `npm run build`, `npm test`, `npm run test:e2e`, `npm -C docs run build`.
2. Buyer dry run, operator dry run, and fleet smoke are checked in under `reports/`.
3. The OpenClaw fleet view shows 3 approved hosts with route health, conflicts, and revoke state from one central Beam control plane.
4. Cross-host messaging keeps one canonical Beam nonce/trace and survives the happy path plus conflict/revoke failure drills.
5. Release notes and changelog are updated before any `v1.2.0` tag is cut.

## Evidence

- `reports/1.2.0-buyer-dry-run.md`
- `reports/1.2.0-operator-dry-run.md`
- `reports/1.2.0-fleet-smoke.md`
- `reports/1.2.0-release-smoke.md`

## Go / No-Go

`RELEASED` as `v1.2.0`.

Reasoning:

- the fleet connector now has first-class host enrollment, approval, heartbeats, inventory sync, and route conflict handling in the central Beam control plane,
- the dashboard exposes a dedicated OpenClaw fleet surface plus workspace host badges and per-identity route state,
- the buyer, operator, fleet, and final release smoke passes are all green and checked into the repo,
- the duplicate conflict and revoked-host failure modes are no longer theoretical; they are exercised in the shipped release path,
- GitHub release, npm publish, and live API release truth are all aligned on `v1.2.0`.
