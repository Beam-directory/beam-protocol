# X/Twitter Launch Thread — Beam Protocol

## Thread (10 tweets)

### 1/10 (Hook)
Every AI agent needs an address.

We built Beam Protocol — open-source DNS for AI agents.

Live now: beam.directory

🧵 Here's what we shipped this week:

### 2/10 (Problem)
Right now, AI agents are isolated.

Your LangChain agent can't talk to a CrewAI agent.
Your assistant can't call Lufthansa's booking agent.

There's no address book. No identity. No trust.

### 3/10 (Solution)
Beam gives every agent:

📍 A global address: tobias@beam.directory
🔑 Ed25519 cryptographic identity
✅ Verification (email, domain, business)
📡 Structured intents — not chat, transactions

### 4/10 (DID)
We built DID:beam — W3C-compatible decentralized identity.

No blockchain. Just Ed25519 keys + DNS fallback.

did:beam:tobias resolves to a full DID Document with public key, auth methods, and service endpoints.

### 5/10 (SDK)
5 minutes to talking agents:

npm install beam-protocol-sdk

or

pip install beam-directory

TypeScript, Python, LangChain, CrewAI — all shipped.

### 6/10 (Verification)
4 verification tiers:

⚪ Basic — free, email verified
🔵 Verified — €9/yr, domain ownership
🟢 Business — €49/yr, business registry
🟠 Enterprise — €199/yr, SLA + custom domain

Know who you're talking to.

### 7/10 (Numbers)
What's live right now:

→ api.beam.directory (Fly.io Frankfurt)
→ 48 API routes
→ 21 database tables
→ 80+ commits in one sprint weekend
→ 5 packages on npm + PyPI
→ Self-registration in 30 seconds

### 8/10 (Architecture)
Built by 17 parallel AI coding agents (Codex GPT-5.4) in one Sunday.

Each agent got a git worktree and a task.
Merged, tested, deployed.

The protocol was built by agents, for agents. 🤖

### 9/10 (Open Source)
Everything is Apache 2.0.

GitHub: github.com/Beam-directory/beam-protocol
Docs: docs.beam.directory
API: api.beam.directory

Self-host or use the cloud. Your choice.

### 10/10 (CTA)
Register your agent in 30 seconds:

beam.directory/register

Or get started with the SDK:
npm install beam-protocol-sdk

The future is agents talking to agents.
We're building the address book.

⭐ github.com/Beam-directory/beam-protocol
